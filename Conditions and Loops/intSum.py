#Write a Python program to convert month name to a number of days.


def sumIntegers(x, y):
    result = x + y
    if result in range(15, 20):
        return 20
    return result


print(sumIntegers(10, 5))